[build]
  command = "npm run build"
  publish = "dist"

# Configuração para SPA (Single Page Application)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Habilitar Netlify Identity para DecapCMS
[build.environment]
  NODE_VERSION = "18"
